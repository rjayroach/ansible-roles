---
common:
  homebrew_install_path: /usr/local

  homebrew_installed_packages:
    - brew-cask
    - fleetctl

  homebrew_taps:
    - caskroom/cask
    - homebrew/binary

  homebrew_cask_appdir: /Applications

  homebrew_cask_apps:
    - boot2docker
    - insomniax
    - iterm2
    - vagrant
    - virtualbox
