{{ ansible_managed | comment }}

apiVersion: v1
kind: Config
clusters:
- cluster:
    certificate-authority: {{ kubernetes_var_path }}/ca.pem
    server: https://{{ hostvars[groups['controller'][0]]['ansible_' + iface].ipv4.address }}:6443
  name: kubernetes
contexts:
- context:
    cluster: kubernetes
    user: kubelet
  name: kubelet
current-context: kubelet
users:
- name: kubelet
  user:
    token: chAng3m3
