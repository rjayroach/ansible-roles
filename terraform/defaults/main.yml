---
terraform_version: 0.11.3
terraform_file: 'terraform_{{ terraform_version }}_linux_amd64'
terragrunt_version: 0.14.0
task: noop

# next var for template, but not in use now
tf_creds_dir: '{{ credentials_dir }}/terraform'
