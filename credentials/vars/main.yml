---
credentials:
  src_dir: '{{ credentials_src_dir }}'
  src_pattern: "{{ credentials_src_pattern | default('csv') }}"
  project_names: '{{ credentials_project_names }}'
  provider: '{{ credentials_provider }}' # aws, github, docker, k8s, etc
